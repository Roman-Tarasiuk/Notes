export class Note {
    title: string;
    description: string;
    text: string;

    constructor(title: string, description: string, text: string) {
        this.title = title;
        this.description = description;
        this.text = text;
    }
}